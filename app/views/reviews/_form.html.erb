<%= simple_form_for [@restaurant, @review] do |f| %>

<%= f.error_notification %>

<div class="form-inputs">
  <%= f.input :content %>
  <%= f.input :rating, type: :integer %>
</div>

<div class="form-actions">
  <%= f.button :submit %>
</div>

<% end %>
